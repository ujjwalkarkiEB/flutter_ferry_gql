targets:
  $default:
    builders:
      ferry_generator|graphql_builder:
        enabled: true
        options:
          schema: auhentication_gql|lib/graphql/schema.graphql
          output_dir: "gql_g_files"
      ferry_generator|serializer_builder:
        enabled: true
        options:
          schema: auhentication_gql|lib/graphql/schema.graphql
          output_dir: "gql_g_files"

