query GetPostById($id: String!) {
    getPostById(postId: $id) {
        user {
            _id
            userName
        }
        title
        description
        commentCount
        likeCount
        image
        createdAt
        updatedAt
    }
}